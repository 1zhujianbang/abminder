{
  "name": "abminder",
  "version": "1.0.0",
  "private": true,
  "description": "abminder — 交易信号标注工具",
  "scripts": {
    "dev": "node server.js",
    "deploy": "npx wrangler pages deploy . --project-name=abminder"
  }
}
